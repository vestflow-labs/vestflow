# For the frontend, I am expected to update `app/app/bulk-create/page.tsx` with a Merkle mode toggle.
# Since it might be complex to modify this file safely with sed, I will just append a comment at the top for now
# or maybe write a quick react component addition. 
# Let me use the `replace_file_content` or python.
